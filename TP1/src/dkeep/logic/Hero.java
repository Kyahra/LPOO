package dkeep.logic;

public class Hero {

	private	char hero = 'H';

	private int hero_x;
	private int hero_y;
	/*
	public void moveHero(int new_x, int new_y) {

		
		if (map_1[new_x][new_y] == 'X' || map_1[new_x][new_y] == 'I'){

			return;
		}

		if (map_1[new_x][new_y] == ' ') {

			if (lever) {
				map_1[hero_x][hero_y] = 'k';
				lever = false;
			} else
				map_1[hero_x][hero_y] = ' ';

			map_1[new_x][new_y] = 'H';

			hero_x = new_x;
			hero_y = new_y;
			
			move_guard = true;
			
			return;

		}
		
		if (map_1[new_x][new_y] == 'S') {
			stair = true;
			
			map_1[new_x][new_y] = 'H';

			hero_x = new_x;
			hero_y = new_y;
			
			
			return;

		}

		if (map_1[new_x][new_y] == 'k') {
			lever = true;
			openDoors();

			map_1[hero_x][hero_y] = ' ';

			map_1[new_x][new_y] = 'H';
			
			hero_x = new_x;
			hero_y = new_y;
			
			move_guard = true;
			return;

		}
		

	}
	*/
}
